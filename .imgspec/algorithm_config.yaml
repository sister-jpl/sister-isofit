# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: sister-isofit
version: 1.0.0
environment: ubuntu
repository_url: https://gitlab.com/geospec/sister-isofit.git
# TODO: Get correct docker base url
docker_url: registry.imgspec.org/root/ade_base_images/isofit:latest
build_command: sister-isofit/.imgspec/install.sh

# fill out these fields
# explain what this algorithm does
description: The SISTER wrapper for ISOFIT. ISOFIT (Imaging Spectrometer Optimal FITting) contains a set of routines 
  and utilities for fitting surface, atmosphere and instrument models to imaging spectrometer data.
# path to the wrapper script for running the algorithm
run_command: sister-isofit/.imgspec/imgspec_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 70GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: l1_granule
    type: file
  - name: surface_reflectance_spectra
    type: positional
  - name: vegetation_reflectance_spectra
    type: positional
  - name: water_reflectance_spectra
    type: positional
  - name: snow_and_liquids_reflectance_spectra
    type: positional
  - name: segmentation_size
    type: positional
    default: 50
  - name: n_cores
    type: positional
    default: 32
