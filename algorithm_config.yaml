# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algorithm_name: sister-isofit
algorithm_version: sister-dev
repository_url: https://gitlab.com/geospec/sister-isofit.git
docker_container_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit
# See corresponding Dockerfile at https://gitlab.com/geospec/base-images/-/blob/main/isofit/docker/Dockerfile

# fill out these fields
# explain what this algorithm does
algorithm_description: The SISTER wrapper for ISOFIT. ISOFIT (Imaging Spectrometer Optimal FITting) contains a set of routines
  and utilities for fitting surface, atmosphere and instrument models to imaging spectrometer data.
build_command: sister-isofit/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-isofit/run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  file:
    - name: radiance_dataset
      required: True
    - name: location_dataset
      required: True
    - name: observation_dataset
      required: True
  config:
    - name: segmentation_size
      default: "50"
    - name: n_cores
      default: "32"
    - name: crid
      default: "000"
